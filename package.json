{
  "name": "dynastytrust-phase1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "dev": "npm run dev --workspace @dynastytrust/web",
    "test": "node scripts/test-policy.mjs"
  }
}
