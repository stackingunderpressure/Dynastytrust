[package]
name = "dynastytrust_protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
bitcoin = { version = "0.31", features = ["serde"] }
miniscript = { version = "11", features = ["serde", "compiler"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
hex = "0.4"
