import crypto from 'node:crypto';
export interface EncryptedPrivateKeyBlob { version: 1; cipher: 'aes-256-gcm'; nonceB64: string; ciphertextB64: string; authTagB64: string; }
function masterKey(): Buffer { const hex = process.env.APP_MASTER_KEY; if (!hex) throw new Error('APP_MASTER_KEY is required'); const key = Buffer.from(hex, 'hex'); if (key.length !== 32) throw new Error('APP_MASTER_KEY must be 32 bytes'); return key; }
export function encryptPrivateHex(privateHex: string): EncryptedPrivateKeyBlob { const nonce = crypto.randomBytes(12); const cipher = crypto.createCipheriv('aes-256-gcm', masterKey(), nonce); const ciphertext = Buffer.concat([cipher.update(privateHex, 'utf8'), cipher.final()]); return { version: 1, cipher: 'aes-256-gcm', nonceB64: nonce.toString('base64'), ciphertextB64: ciphertext.toString('base64'), authTagB64: cipher.getAuthTag().toString('base64') }; }
export function decryptPrivateHex(blob: EncryptedPrivateKeyBlob): string { const decipher = crypto.createDecipheriv('aes-256-gcm', masterKey(), Buffer.from(blob.nonceB64, 'base64')); decipher.setAuthTag(Buffer.from(blob.authTagB64, 'base64')); const plaintext = Buffer.concat([decipher.update(Buffer.from(blob.ciphertextB64, 'base64')), decipher.final()]); return plaintext.toString('utf8'); }
