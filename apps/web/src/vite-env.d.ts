/// <reference types="vite/client" />

// Baked in by vite.config.ts's `define` at build time -- the short
// (7-char) commit SHA Netlify sets as COMMIT_REF, or 'dev' locally.
declare const __APP_VERSION__: string;
