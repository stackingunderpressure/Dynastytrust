[workspace]
resolver = "2"
members = ["protocol", "backend", "cli", "compiler"]

[profile.release]
# Catch integer overflow in release builds (important for PSBT arithmetic)
overflow-checks = true
opt-level = 3
lto = true
codegen-units = 1
