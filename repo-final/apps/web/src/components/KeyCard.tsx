import type { PublicKeyRecord } from '../api';
export default function KeyCard({ keyRecord, onArchive, onCompromise }: { keyRecord: PublicKeyRecord; onArchive: (id: string) => void; onCompromise: (id: string) => void; }) { return <div style={{ border: '1px solid #ccc', borderRadius: 12, padding: 16, marginTop: 12 }}><div style={{ fontWeight: 700 }}>{keyRecord.label}</div><div>Origin: {keyRecord.origin}</div><div>Network: {keyRecord.network}</div><div>Status: {keyRecord.status}</div><div>Fingerprint: {keyRecord.fingerprint ?? 'n/a'}</div><div>Path: {keyRecord.derivationPath ?? 'n/a'}</div><div style={{ wordBreak: 'break-all' }}>XPUB: {keyRecord.xpub ?? 'n/a'}</div><div style={{ wordBreak: 'break-all' }}>Pubkey: {keyRecord.pubkey}</div><div style={{ display: 'flex', gap: 8, marginTop: 12 }}><button onClick={() => onArchive(keyRecord.keyId)}>Archive</button><button onClick={() => onCompromise(keyRecord.keyId)}>Mark compromised</button></div></div>; }
